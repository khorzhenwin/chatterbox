`vibe-voice/` keeps project-local VibeVoice assets.

- Put downloaded model weights under `vibe-voice/models/` if you use `make vibe-download`.
- Put optional speaker reference clips under `vibe-voice/voices/`.
- Generated audio still goes to `output/vibe-voice/`.
